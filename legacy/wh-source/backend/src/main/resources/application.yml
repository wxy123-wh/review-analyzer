server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/reputation_mvp?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: repu
    password: repu123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql

openai:
  api:
    base-url: https://api.jiekou.ai/openai
    key: sk_1JedsI0DA6M7vZiEpQ_YQ7NokHiE1LBXhC48rzsGsvY
    model: deepseek/deepseek-v3.1
    timeout: 60

# NLP服务配置
nlp:
  service:
    url: http://localhost:8000
    enabled: true  # 是否自动启动NLP服务
    port: 8000     # NLP服务端口

# 聚类分析配置
clustering:
  auto-run-on-startup: true  # 是否在应用启动时自动执行聚类

