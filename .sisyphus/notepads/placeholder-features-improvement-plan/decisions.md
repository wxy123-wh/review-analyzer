## Session Notes

- 2026-04-06 cleanup: reverted every non-Task-1 file and kept the smallest viable contract slice—taxonomy aliases, module inventory, truthful showcase notes, analysis-job wording, and matching tests/docs only.
## Session Notes

- 2026-04-06: Kept runtime behavior intentionally narrow for Task 1; did not implement analysis execution or materialized-query consumption, but locked the contract in code comments and docs so later tasks have one authoritative semantic target.
- 2026-04-06: Removed canonical taxonomy drift from touched compare/client paths by replacing `audio`, `connectivity`, and `noise_canceling` with the chosen v1 aspect names.
- 2026-04-06: Used showcase `note` fields plus the docs inventory as the explicit truth source for placeholder vs controlled-data-only vs gated-placeholder classification.

- 2026-04-06: Kept Task 2 execution on the existing `/api/v1/analysis/start` path and made it synchronous instead of adding async infrastructure, because the repo has no current executor/scheduler pattern and the task only requires a real lifecycle plus persisted outputs.
- 2026-04-06: Reused the existing schema tables for analysis materialization (`review_aspects`, `issue_clusters`, `issue_scores`) and did not create a parallel result model; `validation_metrics` remains out of this slice because current validation is still action-driven compute-on-read.
- 2026-04-06: Kept Task 3 on the existing analysis-job API shape and reused `AnalysisJobResponse.errorMessage` as the stable carrier for degraded notices on otherwise successful jobs, rather than widening the read contract before later tasks define a dedicated notice field.
- 2026-04-06: Preserved backend query/materialization scoring semantics by consuming NLP aspect + polarity + confidence, but still deriving persisted sentiment scores and issue-priority math from the backend’s existing rules so downstream consumers stay stable.
- 2026-04-06: For Task 4, reused the existing `LoginGate -> enter -> App.handleLogin()` flow rather than adding auth infrastructure; only the gate inputs moved to env-backed props so tests and user interaction stay stable.
- 2026-04-06: For Task 4, kept backend CORS on the existing `WebConfig` path but replaced `allowedOriginPatterns("*")` with `app.cors.allowed-origins` configuration so safe localhost defaults and deployment-specific origins share one Spring properties entry point.
- 2026-04-06: For Task 5, kept the cutover inside `InsightQueryService` plus repository read helpers instead of creating a parallel query service, and preserved the frontend contract by mapping missing materialized outputs to the existing issue fallback row / trend notice / wordcloud notice semantics.
- 2026-04-06: For Task 7, kept the outward validation payload unchanged (`actionId`, before/after rates, improvement rate, summary) and moved traceability into persisted `validation_metrics` rows plus `improvement_actions.launched_at`, rather than widening the UI contract.
- 2026-04-06: For Task 7, reused the existing `ActionService` / `ActionRepository` path for action list/detail exposure and added only the smallest extra repository context needed for validation window resolution, instead of inventing a separate action-read model.
- 2026-04-06: For Task 8, kept `compare`, `trends`, and `wordcloud` on their existing contract semantics, but normalized the dashboard orchestration so `overview`, `issues`, `actions`, and `validation` now consume explicit frontend contract state and surface degraded notices instead of silently collapsing to empty-success UI.
- 2026-04-06: For Task 10, reused the existing `success / empty / degraded / error` core contract vocabulary instead of inventing a brand-new literal enum, and only widened the frontend union with `disabled` / `runtime-unavailable` where the UI already has genuine gating or showcase-runtime semantics.
- 2026-04-06: For Task 10, kept showcase runtime sources anchored to real persisted job/materialization/action/validation signals; the contract upgrade standardizes interpretation, not data provenance.
- 2026-04-06: For Task 11, added focused component/service tests instead of widening `App.spec.ts` or building a new cross-service harness, because the repo already exposes the highest-value compatibility seam in `AnalysisJobService` and the showcase panels each own non-trivial derived-state rendering that deserves isolated assertions.
- 2026-04-07: For Task 12, kept `aggregator-demo` on the existing controlled-data `QUEUED` path and treated only `onebound` as the external second track; every other provider and every non-default onebound platform is now explicitly terminal-and-unsupported rather than ambiguously queued.
- 2026-04-07: For Task 12, added only manual-handoff readiness metadata (`analysis_handoff_status` / `analysis_handoff_note`) to `sync_jobs` instead of building an automatic sync→analysis pipeline, because the task requires structural readiness without changing the controlled-data-first launch behavior.

- 2026-04-07: For Task 13, kept the doc update narrowly scoped to README plus the two primary docs, and described showcase / explainability / chaos using the exact backend and frontend contract vocabulary already present in `ShowcaseService`, `App.vue`, and `showcaseCopy.ts` instead of inventing a parallel documentation taxonomy.
