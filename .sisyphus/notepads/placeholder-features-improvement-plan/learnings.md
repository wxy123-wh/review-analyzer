## Session Notes

- 2026-04-06 cleanup: reverted all out-of-scope tool state, screenshots, evidence deletions, and unrelated product files; retained only Task 1 contract files plus notepad notes.

- 2026-04-06: Wave-1 contract now fixes canonical aspect taxonomy to `battery` / `bluetooth` / `noise-canceling` / `comfort` / `microphone`; frontend client normalizes legacy aliases (`connectivity`, `noise_canceling`, `call_quality`) before consumption.
- 2026-04-06: `frontend/src/App.vue` now carries an explicit module inventory covering state, data source, and keep/replace/hide strategy for every core/showcase module.
- 2026-04-06: `showcase/explainability` is documented and coded as `controlled-data-only` rather than a full placeholder because it currently explains deterministic score weights, not model introspection.

- 2026-04-06: Task 2 now executes analysis jobs synchronously through `QUEUED -> RUNNING -> SUCCEEDED/FAILED`, using demo-seeded `reviews_raw` as the controlled source and persisting materialized outputs into `review_aspects`, `issue_clusters`, and `issue_scores`.
- 2026-04-06: Explicit reuse semantics for controlled analysis are: return the latest successful job for the same product only when `reviews_raw.fetched_at` has not advanced past that job's `finished_at` and persisted aspect rows still exist for the product.
- 2026-04-06: Final Task 2 verification pass confirmed the backend slice stays on the existing `/api/v1/analysis/start` path with terminal metadata surfaced via `finishedAt` / `errorMessage`; local verification remains limited by missing `jdtls` and Maven binaries in this session, so command attempts must be reported as environment blockers rather than green runs.
- 2026-04-06: Task 3 now routes `AnalysisJobService` through a dedicated `NlpReviewAnalysisClient` that posts the existing review batch to `/analyze`, validates the reply shape by `jobId` and `reviewIndex`, and degrades back to controlled analysis when the NLP service times out, is unavailable, or returns an unusable payload.
- 2026-04-06: The shared aspect language for backend materialization and `nlp-service` output is now the canonical set `battery`, `bluetooth`, `noise-canceling`, `comfort`, `microphone`; legacy labels like `connectivity`, `noise_canceling`, and `call_quality` remain accepted only as aliases during normalization, not as emitted output keys.
- 2026-04-06: Task 4 keeps the existing frontend-only login interaction, but the accepted username/password/display text now come from `VITE_INTERNAL_ACCESS_*` variables so the entry path is internal-use and environment-configured instead of a component-local demo constant.
- 2026-04-06: Task 4 surfaces placeholder / controlled-data / gated module state directly in `AppShellSidebar`, and keeps `VITE_SHOW_CHAOS_MODULE` as the actual enable switch while showing a non-clickable hidden-module card when that module is gated off.
- 2026-04-06: Task 5 switches `InsightQueryService` query reads to `AnalysisMaterializationRepository`: issues now consume `issue_clusters + issue_scores`, trends consume `review_aspects` joined to `reviews_raw.review_time`, and wordcloud tokenization only runs on reviews already scoped by materialized aspect/sentiment rows.
- 2026-04-06: Task 7 now closes the action→validation loop by persisting action-linked validation snapshots in `validation_metrics` and reusing them on later reads instead of recalculating a generic first-half/second-half demo split on every request.
- 2026-04-06: Task 7 also promotes existing backend action repository/service capabilities into real `GET /api/v1/actions` and `GET /api/v1/actions/{actionId}` endpoints, letting the frontend hydrate action state from backend persistence instead of session-only memory.
- 2026-04-06: Task 8 replaces raw-array frontend reads for `issues`, `actions`, and `validation` with explicit contract objects carrying `state + items + notice`, so `App.vue` and the core panels can distinguish `success` / `empty` / `degraded` / `error` without relying on built-in demo payload assumptions.
- 2026-04-06: Task 10 replaces synthetic issue fallback rows with explicit backend `state + notice` for issues, trends, wordcloud, and validation, so empty/failure/degraded semantics survive transport without depending on fabricated list items.
- 2026-04-06: Task 10 keeps compare's special states (`missing-target`, `comparison-unavailable`, `primary-unavailable`) but treats showcase `LIVE / DEGRADED / CONTROLLED_DATA_ONLY / RUNTIME_UNAVAILABLE` as a mapped frontend meaning set alongside core contract states instead of a separate ad-hoc vocabulary.
- 2026-04-06: Task 11 now locks showcase panel behavior with dedicated unit specs for pipeline, agent arena, explainability, chaos, and report center, so panel-local derived metrics and degraded/runtime-unavailable copy no longer depend only on broad `App.spec.ts` navigation coverage.
- 2026-04-06: Task 11 also hardens the in-repo backend↔NLP compatibility seam by asserting that `AnalysisJobService` degrades back to controlled analysis when an otherwise well-shaped NLP reply emits an unsupported aspect key, preserving persisted output stability without new integration harnesses.
- 2026-04-07: Task 12 keeps second-track external-source ingestion on the existing `reviews_raw` table by adding nullable external-source metadata (`provider`, `platform`, `external_product_code`, `sync_job_id`, `external_dedupe_key`, `fetch_metadata`) instead of introducing a parallel raw-review table.
- 2026-04-07: Task 12 changes sync transparency from implicit `QUEUED forever` behavior to explicit terminal metadata: unsupported provider/platform now persist as `status=UNSUPPORTED`, onebound failures persist `status=FAILED`, and the sync API/database now expose `analysisHandoffStatus` + `analysisHandoffNote` for later analysis-lane readiness.

- 2026-04-07: Task 13 aligns README, 接口文档, and 代码文档 to the current runtime truth: controlled-data-first launch is still primary, analysis now runs synchronously with reuse and degrade-success semantics, compare/showcase read real v1 contracts, and external-source ingestion remains a second-track handoff-ready skeleton.
