- 2026-04-04: Chose a container/presentational split using `AppShellFrame.vue`, `AppShellSidebar.vue`, and `AppShellHeader.vue` so later redesign work gets explicit shell seams without moving orchestration out of `App.vue`.
- 2026-04-04: Kept all module content branches, activation handlers, and lazy loading calls in `App.vue` to preserve the current single-shell interaction model and avoid routing/store rewrites.
- 2026-04-04: Reduced the extraction to the original shell structure and baseline shell styling only; any motion, decoration, or shared-component visual changes were treated as out of scope and removed.
- 2026-04-04: Kept Task 2’s token foundation entirely in `frontend/src/styles.css` instead of adding a separate theme module, because the repo already relies on scoped CSS and CSS custom properties give every Vue SFC a shared vocabulary with minimal wiring.
- 2026-04-04: Established the dark premium foundation as stepped canvas/surface tokens, border tiers, restrained accent gradients/glow, semantic status colors, and shared spacing/radius/type/motion/shadow tokens, then applied it only to shell chrome, login, and `StatusCard.vue` to avoid drifting into broader redesign work.
- 2026-04-04: Wired Task 9 motion preferences at the app root with `useMotionPreferences()` in `frontend/src/App.vue` so the global `html[data-motion]` state stays active across both auth and shell surfaces, instead of relying on a login-only consumer.
- 2026-04-04: Kept the foundational motion consumers minimal: reveal primitives on login and sidebar entry points, hover/spotlight primitives on existing interactive controls, and a shell-enter primitive on `AppShellFrame.vue`, avoiding any new parent-level GSAP selector animation or broad visual redesign.
- 2026-04-04: For the flagship auth refresh, chose a two-column editorial composition: identity/context rail on the left and a compact credentials panel on the right, so the demo form stays primary while truthful context and the monster system carry the premium atmosphere without implying unsupported SSO or security features.
- 2026-04-05: For Task 4, chose a compact operator-console shell direction built entirely from the existing dark tokens and `data-motion-*` primitives, avoiding any new theme layer or bespoke animation system so the refreshed chrome still reads as part of Task 2 and Task 9.
- 2026-04-05: Split sidebar items into visual groups based on the existing module ids (`showcase-*` versus baseline modules) inside `AppShellSidebar.vue`, because that improved navigation hierarchy without altering item labels, click behavior, or the `nav-${module.id}` test contract.
- 2026-04-05: Kept Task 5 inside the primitive layer by reusing the existing gradient surface, border tiers, pill labels, and light `data-motion-hover` affordances already present in the shell; no new tokens or parent wiring were added, and each reusable card/list surface now carries only one strong semantic accent at a time.
- 2026-04-05: For Task 6, kept `IssueTable.vue` and `CompareTable.vue` self-contained and reused the existing shell tokens locally instead of adding new shared primitives or behavior, because the requirement was a pure readability restyle of two dense tables without widening scope into adjacent modules.
- 2026-04-05: Chose compact pill treatments for aspect/score/gap values and restrained row hover states, so the first identifying column and numeric hierarchy become easier to scan on dark surfaces while `priorityScore.toFixed(4)`, compare score formatting, and the existing up/down gap semantics remain untouched.
- 2026-04-05: For Task 7, kept both chart panels self-contained and limited script changes to dark-safe chart color tuning only where the new panel surfaces required readability; the branch structure, retry emits, lazy imports, destroy-before-rerender flow, resize cleanup, and test-mode conditions stayed untouched so the redesign remained a container pass rather than a behavior rewrite.
- 2026-04-05: For Task 8, chose a consistent two-step composition across all four showcase panels—hero framing first, structured detail second—so the post-login showcase reads as one premium suite while keeping each module’s original heading, payload semantics, and action path intact.
- 2026-04-05: Kept `frontend/src/utils/showcaseCopy.ts` unchanged because the existing `演示数据` normalization already preserves the truth contract, and the panel upgrade could rely entirely on that shared wording plus the backend/client-provided `note` fields.
