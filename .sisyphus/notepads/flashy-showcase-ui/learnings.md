- 2026-04-04: `App.vue` can be safely split by extracting the authenticated shell frame, sidebar, and header into child components while keeping auth state, module registry, activation, and lazy data loading in the parent.
- 2026-04-04: Existing app behavior is locked primarily by `frontend/src/test/App.spec.ts`; keeping `login-gate`, `narrow-sidebar`, and `nav-*` selectors stable allowed the shell extraction to pass without test changes.
- 2026-04-04: Verification for this extraction succeeded with `npm --prefix frontend test -- App.spec.ts` and `npm --prefix frontend run build`; an ad-hoc `tsc --noEmit` still reports pre-existing frontend typing/environment gaps unrelated to the shell split.
- 2026-04-04: For Task 1, a safe extraction means moving the original shell layout/styles into shell components, not introducing extra transitions, decoration slots, or shared-component animation updates.
- 2026-04-04: Removing the `App.vue` mount-time GSAP selector animation eliminated the `GSAP target .status-card not found` warning in the focused app test without affecting auth or module behavior.
- 2026-04-04: `frontend/src/styles.css` was effectively empty before Task 2, so it works well as the single global token source; scoped Vue SFC styles can consume `var(--...)` directly without introducing a JS theme layer.
- 2026-04-04: `LoginGate.vue` remains the strongest spacing/form-control reference, while `AppShellFrame.vue`, `AppShellHeader.vue`, `AppShellSidebar.vue`, and `StatusCard.vue` can safely adopt shared dark tokens without changing selectors or component APIs.
- 2026-04-04: Task 9 only became real once `frontend/src/motion/primitives.css` was imported from `frontend/src/main.ts` and existing auth/shell surfaces consumed `data-motion-*`; unused motion files alone did not change runtime behavior or test outcomes.
- 2026-04-04: `AnimatedCharacters.vue` can adopt `useMotionLifecycle()` safely for timer and RAF cleanup while still preserving the selectors and class timing that `frontend/src/test/App.spec.ts` asserts, as long as test-mode suppression only disables the random blink/peek/look loops rather than the click and pupil-tracking behaviors.
- 2026-04-04: The login redesign can feel substantially more premium without touching auth semantics by keeping the compact `.form` intact, moving truthful demo framing into supporting cards, and elevating `AnimatedCharacters.vue` inside a dedicated stage frame that still passes through the existing props unchanged.
- 2026-04-05: Task 4 can materially improve shell hierarchy without touching `App.vue` orchestration by strengthening only the shell seams: grouped sidebar sections in `AppShellSidebar.vue`, a denser utility-style header in `AppShellHeader.vue`, and a more embedded `.module-card` surface in `App.vue` while leaving `activeModule`, `activateModule()`, and `ensureModuleData()` untouched.
- 2026-04-05: The safest strong active-state treatment for this dashboard shell was a single persistent cue inside each existing `nav-*` button (a slim accent rail plus brighter tile/dot treatment), which made navigation feel more premium without renaming hooks or changing button semantics.
- 2026-04-05: For Task 5, `StatusCard.vue` worked best as the shared primitive reference when its status cue stayed singular and compact: one semantic badge plus a matching border/shadow signal, while `ActionList.vue` and `ValidationList.vue` adopted the same dark surface, pill metadata, and denser row rhythm without changing their contracts.
- 2026-04-05: Dense dashboard tables fit the premium baseline best when they keep the same dark panel shell and dashed empty-state framing as the newer list/card primitives, but shift emphasis inside the grid toward header hierarchy, first-column scanning, compact value pills, and subtle horizontal separators rather than louder row fills.
- 2026-04-05: Task 7’s chart containers landed safely when the premium treatment stayed outside the plotting area: dark panel shells, compact legend/support rows, and inset fallback/detail cards can modernize `TrendList.vue` and `WordCloudPanel.vue` without disturbing test-mode fallback rendering or the existing G2Plot lifecycle.
- 2026-04-05: Task 8 could make the showcase modules feel substantially more premium without touching `App.vue` orchestration by treating the incoming demo payloads as staged data sources: hero framing, spotlight cards, and structured secondary detail all came from existing fields like `stages`, `agents`, `drills`, `previewSections`, and truthful `note/status` copy.
- 2026-04-05: `frontend/src/test/App.spec.ts` only locks the showcase module names and key visible copy, so the safest redesign path was to preserve headings like `流水线编排` and the report CTA text `生成演示数据报告预览` exactly while changing panel composition underneath.
- 2026-04-05: Task 10 landed best when hardening focused on seams instead of restyling: moving the login form panel ahead of the decorative stage on tablet/mobile, adding shared visible focus treatment through `frontend/src/styles.css`, and preserving existing selectors/copy let accessibility improve without disturbing demo auth semantics.
- 2026-04-05: Dense data surfaces stayed readable when premium effects were pulled back from their containers: `App.vue` no longer spotlights the entire module card, `IssueTable.vue` now prefers horizontal scroll over squeezed columns on mobile, and `ShowcaseChaosPanel.vue` keeps the soft spotlight only on the hero instead of the full panel.
- 2026-04-05: Task 11 redesign coverage stayed deterministic when tests anchored on existing selectors and exposed state hooks instead of animation progress: `App.spec.ts` can safely lock auth/shell behavior through `login-gate`, `narrow-sidebar`, `nav-*`, `aria-current`, and the test-mode root attribute `data-motion="none"`, while `TrendList.vue` and `WordCloudPanel.vue` are best protected through their fallback DOM, legends, summaries, and retry states because test mode intentionally skips live chart rendering.
- 2026-04-05: The final visual-language gap was `frontend/src/components/ShowcaseExplainabilityPanel.vue`; aligning it to the shared dark panel shell, pill treatment, and inset detail-card rhythm was enough to make auth, shell, dense data, and showcase screens read as one product without touching `App.vue` orchestration or introducing a new motif.
